# VPS publish — example (safe to commit)

Copy to `config.local.md` and fill in real values. `config.local.md` is gitignored.

```yaml
host: 203.0.113.10
user: root
password: ""          # optional; prefer SSH key
path: /opt/jamejafari/jamejafari
ssh_key: ""           # optional, e.g. C:\Users\You\.ssh\id_ed25519
```
